{
  "name": "vscode-twin-macro-autocomplete",
  "displayName": "Auto complete for twin.macro",
  "version": "0.0.3",
  "publisher": "denvash",
  "repository": {
    "type": "git",
    "url": "https://github.com/denvash/tailwind-macro-autocomplete-poc"
  },
  "bugs": {
    "url": "https://github.com/denvash/tailwind-macro-autocomplete-poc/issues"
  },
  "engines": {
    "vscode": "^1.32.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "lint": "tslint -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^12.12.0",
    "@types/vscode": "^1.32.0",
    "typescript": "^3.8.3"
  }
}
